# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: aaarora/rucio-sense-xrootd:latest 

redi:
  replicaCount: 1
  resources: {}
  nodeSelector: {}

server:
  replicaCount: 1
  resources: {}
  nodeSelector: {}
